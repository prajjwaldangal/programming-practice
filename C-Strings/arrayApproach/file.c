#include "file.h"
#include "stdlib.h"
/* Allocates and initializes a new list. */
list* create_list() {
    // return (list *) malloc(sizeof(list));
    list * myList;
    return myList;
}

int main() {
    list * newList = create_list();
}